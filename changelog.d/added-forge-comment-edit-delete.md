- **Edit and delete your own comments** on GitLab merge requests and issues, and
  on Bitbucket pull requests — the same inline editor and delete confirmation that
  GitHub comments already had, now wired to each provider's native commands.
